services:
  app:
    container_name: synk_queuer
    build: .
    ports:
      - ${PORT}:${PORT}
    volumes:
      - ./:/app
      - ./.cert:/cert
    command: go run .
    env_file: .env

    networks:
      - synk_network

networks:
  synk_network:
    external: true